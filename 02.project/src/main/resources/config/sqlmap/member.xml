<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.mlec.repository.mapper.MainMapper">
	<resultMap id="memberMap" type="member">
		<result column="id_no" property="idNo" />
		<result column="userid" property="userid" />
		<result column="password" property="password" />
		<result column="phone_no" property="phoneNo" />
		<result column="addr" property="addr" />
		<result column="birth_date" property="birthDate" />
		<result column="log_on" property="logOn" />
	</resultMap>
	
	<select id="selectmember" resultMap="memberMap" parameterType="String">
	
		select *
		  from mybox_member
		  where userid = #{userid}
		 order by no desc 
	</select>
	<!-- <select id="loginMain" resultMap="mainMap" parameterType=""></select> -->
	<insert id="insertmember" parameterType="member">
		insert into member(
	userid, password, phone_no, addr, birth_date, log_on
				)values(
		 #{userid}, #{password}, #{phoneNo}, #{addr}, #{birthDate}, #{n}
			)
	</insert> 
	
	<update id="updatemember" parameterType="member">
	update mybox_member(
			set userid=#{userid}
				password=#{password}
				phone_no=#{phoneNo}
				addr=#{addr}
				birth_date=#{birthDate}
				
			)
	
	
	</update>
	
	
	
	
	
</mapper>

























